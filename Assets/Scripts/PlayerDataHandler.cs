using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerDataHandler : MonoBehaviour
{
    // Static class that holds the players information

    /*[SerializeField] public static PlayerDataHandler Instance;
    [SerializeField] public string playerName;

    private void Awake()
    {
        if (Instance != null)
        {
            Destroy(gameObject);
        }
        Instance = this;
        DontDestroyOnLoad(gameObject);
    }
    */
}
